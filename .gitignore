# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
*.spec
.venv/
venv/

# Resume state + run log written at runtime
komoot_upload_state.json
komoot_upload_log.txt
*.json.tmp

# Local GPX backups of tours saved before deletion (private track data)
komoot_tour_backups/

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Markdown docs kept local only — except the public README.
*.md
!README.md

# Local-only sample data and run logs
examples/
LOG.md
LOG_*.md
*_LOG.md
